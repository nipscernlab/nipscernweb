{
  "name": "nipscern-share-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Cloudflare Worker: on-demand social share images + per-post Open Graph for nipscern.com",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "preview": "node preview.mjs"
  },
  "dependencies": {
    "@jsquash/png": "^3.1.1",
    "@jsquash/webp": "^1.5.0",
    "@resvg/resvg-wasm": "^2.6.2"
  },
  "devDependencies": {
    "sharp": "^0.33.5",
    "wrangler": "^4"
  }
}
